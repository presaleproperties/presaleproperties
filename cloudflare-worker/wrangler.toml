name = "og-proxy"
main = "og-proxy.js"
compatibility_date = "2024-01-01"

# Deploy route — update with your actual zone
# [triggers]
# routes = [
#   { pattern = "presaleproperties.com/*", zone_name = "presaleproperties.com" }
# ]
