name = "og-proxy"
main = "og-proxy.js"
compatibility_date = "2024-01-01"

# Routes are LIVE — uncommented and ready to deploy.
# Run `wrangler deploy` from this directory.
[[routes]]
pattern = "presaleproperties.com/*"
zone_name = "presaleproperties.com"

[[routes]]
pattern = "www.presaleproperties.com/*"
zone_name = "presaleproperties.com"
